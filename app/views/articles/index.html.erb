<h1>Articles</h1>
<%= link_to 'Add a new article', new_article_path %>
<ul>
  <% @tasks.each do |article| %>
    <li>
      <%= link_to article.title, article_path(task) %>
      <%= link_to 'Edit', edit_article_path(task) %>
    </li>
  <% end %>
</ul>
